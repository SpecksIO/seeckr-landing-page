{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -E '\\.(js|jsx|ts|tsx|mjs|cjs|mts|json|css)$' | xargs -I{} npx biome check --write --no-errors-on-unmatched {}"
          }
        ]
      }
    ]
  }
}
