# Public base URL of the deployed site.
# Used for metadataBase, canonical URLs, JSON-LD, sitemap.xml, robots.txt and llms.txt.
# Local dev falls back to http://localhost:3000 when unset.
NEXT_PUBLIC_SITE_URL=https://seeckr.com
